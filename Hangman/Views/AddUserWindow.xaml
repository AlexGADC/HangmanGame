<Window x:Class="Hangman.Views.AddUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hangman.Views"
        mc:Ignorable="d"
        x:Name="RootWindow"
        Height="150"
        Width="340"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">

    <Window.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding EnterCommand}" CommandParameter="{Binding ElementName=RootWindow}" />
        <KeyBinding Key="Escape" Command="{Binding CancelCommand}" CommandParameter="{Binding ElementName=RootWindow}" />
    </Window.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Enter your user name:" HorizontalAlignment="Center" FontSize="20"/>
            <TextBlock Text="Only one-word user names allowed*"
                   HorizontalAlignment="Center"
                   FontSize="8"
                   FontFamily="Nirmala UI"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" 
                 MaxLength="30" 
                 FontSize="15" 
                 Width="280"/>
        </StackPanel>

        <StackPanel Grid.Row="2"
                Orientation="Horizontal"
                HorizontalAlignment="Center"
                Margin="0,10,0,35"
                Width="280">

            <Button Content="Enter" 
                Command="{Binding EnterCommand}" 
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" 
                Width="80" 
                Margin="15,0,90,0" 
                Height="20"/>

            <Button Content="Close" 
                Command="{Binding CancelCommand}" 
                CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                Width="80" 
                Height="20"/>
        </StackPanel>
    </Grid>

</Window>